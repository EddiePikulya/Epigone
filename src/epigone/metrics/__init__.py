"""The Metric Library's fine half: fills-derived metrics and Bot vetting (issue #8).

Plain-language definitions of every metric live in docs/metrics.md — keep the
two in sync; that document feeds the criteria-builder explanations.
"""
